import { DomSatanatizerPipe } from './dom-satanatizer.pipe';

describe('DomSatanatizerPipe', () => {
  it('create an instance', () => {
    const pipe = new DomSatanatizerPipe();
    expect(pipe).toBeTruthy();
  });
});
